#include <stdlib.h>

#include "Controlador.h"


int main(int argc, char *argv[]) {
    
    //la clase controlador va a manejar el SVR
    Controlador controlador;
    
    //se llama a la funcion run del controlador 
    return controlador.run(argc, argv);
}
